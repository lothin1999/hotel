# Root Development Dockerfile with Hot Reloading
FROM node:22-alpine

WORKDIR /app/website

COPY website/package*.json ./
RUN npm install --legacy-peer-deps

COPY website/ ./

EXPOSE 4200

CMD ["npx", "ng", "serve", "--host", "0.0.0.0", "--poll", "1000", "--allowed-hosts"]
